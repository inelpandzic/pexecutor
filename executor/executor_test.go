package executor

import (
	"testing"
)

func TestExecutor(t *testing.T) {

	// for i := 0; i < 500; i++ {
	// 	s := rand.NewSource(time.Now().UnixNano())
	// 	r := rand.New(s)
	// 	d := r.Intn(2000)
	//
	// 	t := &executor.Task{
	// 		Name:     fmt.Sprintf("T-%d", i),
	// 		Duration: time.Duration(d) * time.Millisecond,
	// 	}
	//
	// 	e.Submit(t)
	// 	log.Println("MAIN Submited: ", t)
	// }
	//
	// time.Sleep(3 * time.Second)
	// e.Shutdown()
	// log.Println("MAIN - We're done")
}
